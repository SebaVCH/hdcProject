
export enum LocationMethod {
    None = 'none',
    Current = 'current',
    Map = 'map'
}

export enum RouteStatus {
    Active = 'on progress',
    Completed = 'Finalizada'
}

export enum Role {
    volunteer = 'voluntario',
    admin = 'admin'
}